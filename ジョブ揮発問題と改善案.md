# ジョブ揮発問題と改善案

> 記録日: 2026-05-22
> エラー: 「サーバーが再起動してジョブが消えました。もう一度生成してください。」

---

## 現状の問題

アプリは **ジョブの状態をサーバーのメモリ上で管理している** ため、Render 上で以下のタイミングが発生するとジョブが消失する。

| トリガー | 発生頻度 |
|---|---|
| デプロイ（git push） | コード更新のたび |
| 15 分間アクセスなしで sleep（Free プラン） | 頻繁 |
| インスタンスのリスタート | Render 側の都合で随時 |
| メモリ上限超過 | 重い処理時 |

生成には 1〜2 分/人 × 5 人 = **最大 10 分** かかるため、その間に再起動が起きるとユーザー体験が悪化する。

---

## なぜ起きるか

- Flask + Gunicorn（workers=1, threads=4）構成
- ジョブの進捗・結果が Python プロセスのメモリにのみ存在
- プロセスが終了/再起動すると揮発

---

## 改善案（優先度順）

### 案 1: ジョブをファイル / SQLite に永続化（実装コスト：小）

- ジョブ ID、状態、進捗、結果をディスクに保存
- Render のディスク（永続ボリューム）または SQLite を利用
- メモリが揮発しても、起動時にディスクから復元可能
- **最小工数で改善できる**

### 案 2: Redis Queue (RQ) または Celery によるジョブキュー化（実装コスト：中）

- 専用のジョブキューシステムでタスク管理
- ワーカープロセスが Web サーバーと独立
- 再試行・タイムアウト・優先度制御も可能
- Redis のホスティング費用が発生する場合あり

### 案 3: Render Background Worker への分離（実装コスト：中）

- `render.yaml` に `type: worker` を追加
- Web サービスとワーカーサービスを分離
- Web は API のみ、ワーカーが生成処理を担当
- スケーリングと安定性が向上

---

## 当面の対処（運用でカバー）

- 生成中はブラウザを閉じない
- デプロイ直後の生成は避ける
- エラーが出たらリロード → 再生成
- Free プランの sleep を回避するなら、定期的にアクセスする仕組み（UptimeRobot 等）を導入

---

## 次のアクション

- [ ] 案 1（ファイル永続化）から着手するか判断
- [ ] 本番運用するなら案 3（Worker 分離）まで進める
- [ ] それまでは「もう一度生成」で凌ぐ運用

---

## 関連

- 既存メモ: [生成が止まる問題の修正について.md](生成が止まる問題の修正について.md)
- やることリスト: [高野のやることリスト.md](高野のやることリスト.md)
