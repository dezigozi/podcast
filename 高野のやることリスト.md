# 高野のやることリスト（ポッドキャスト生成が止まる問題の対応）

**環境：Mac で次にやる想定**

## 背景
- AIポッドキャスト「知のわんこそば」で、西村哲以外のキャスターを選ぶと30分以上「生成中...」のままになる事象があった
- 原因は Cloudflare Workers の30秒制限。修正として **Cloudflare Queues** を使うように変更済み（コード側の修正は済んでいる）

---

## Mac でやるときの前提
- **ターミナル**：`Terminal.app` または iTerm など
- **プロジェクトの場所**：Dropbox で同期している場合、Mac では例えば  
  `~/Dropbox/会社/仕事/マイドキュメント/Ｍ／G/生産性向上(働き方改革)/AI/■PB/ポッドキャスト`  
  のようなパスになっている想定（要確認）
- **Node.js**：`node -v` と `npm -v` で入っているか確認。未導入なら [nodejs.org](https://nodejs.org/) から LTS を入れる

---

## 高野がやること（未実施のものから）

### 1. GitHub リポジトリへ修正を反映する
- [ ] このフォルダ（または Cursor で修正した内容）の以下を、GitHub の `dezigozi/podcast` リポジトリに反映する
  - `worker/wrangler.toml`
  - `worker/src/index.ts`
- 反映方法：上書きコピー or 差分をマージ or PR 作成

### 2. Mac でプロジェクトフォルダに移動する
- [ ] ターミナルを開き、プロジェクトのルートに移動する
  ```bash
  cd ~/Dropbox/会社/仕事/マイドキュメント/Ｍ／G/生産性向上\(働き方改革\)/AI/■PB/ポッドキャスト
  ```
  （Dropbox のパスが違う場合は、Finder でフォルダを開いてから「パスをコピー」などで確認）

### 3. Worker をデプロイする
- [ ] `worker` ディレクトリに入ってから、以下を実行
  ```bash
  cd worker
  npm install
  npx wrangler deploy
  ```
- Cloudflare にログインしていない場合は `npx wrangler login` を先に実行

### 4. キューが無い場合は作成する
- [ ] デプロイ後、キューが自動作成されていない場合は実行（同じ `worker` ディレクトリで）
  ```bash
  npx wrangler queues create podcast-generate
  ```
  その後、必要なら再度 `npx wrangler deploy`

### 5. 動作確認する
- [ ] ブラウザでポッドキャストの画面を開く
- [ ] 「鈴木健一」や「全員分まとめて生成」で生成を実行
- [ ] 数分以内に「生成完了」になり、音声が聴けることを確認

### 6. （任意）README や運用メモの更新
- [ ] 必要なら README に「生成は Queues 経由で行う」旨を追記
- [ ] 運用メモがある場合は、今回の修正内容・手順を追記

---

## 参考
- 修正内容の詳細：同フォルダの **`生成が止まる問題の修正について.md`**
- リポジトリ：https://github.com/dezigozi/podcast

---

*作成日: 2026-03-16（Mac で次にやる仕様で更新）*
