# 生成が30分ぐるぐるする問題 — 修正内容

## 原因

**Cloudflare Workers には「レスポンス返却後も含めて約30秒」で実行が打ち切られる制限**があります。

- **西村哲（philosopher）** だけは、スクリプト生成＋音声生成がたまたま30秒以内に収まることが多く、完了していた
- **他のキャスター**（鈴木健一など）や **全員分** は処理が長くなり、30秒で Worker が終了
- その結果、バックグラウンドの `processJob` が途中で止まり、KV に「done」が書き込まれず、画面はずっと「生成中...」のまま

## 対応内容

**Cloudflare Queues** を使うように変更しました。

1. **HTTP の `POST /api/generate`**  
   ジョブをキューに送信（`env.PODCAST_QUEUE.send(job)`）してすぐレスポンスを返すだけに変更。  
   → 30秒制限の影響を受けない。

2. **キュー Consumer**  
   キューに届いたメッセージを、**別の Worker 実行**（キュー用）で処理。  
   → キュー Consumer は **最大15分程度** まで実行できるため、全キャスター分の生成が完了できる。

3. **Cron（毎週月曜）**  
   同じく `waitUntil(processJob(...))` ではなく、キューにジョブを送るだけに変更。

## 反映手順（Mac で実施する場合）

1. このフォルダの **`worker/wrangler.toml`** と **`worker/src/index.ts`** を、  
   GitHub の `podcast` リポジトリの同じパスに上書き（またはマージ）する。

2. **ターミナル**でプロジェクトのルートに移動してから、`worker` でデプロイする。
   ```bash
   cd ~/Dropbox/会社/仕事/マイドキュメント/Ｍ／G/生産性向上\(働き方改革\)/AI/■PB/ポッドキャスト
   cd worker
   npm install
   npx wrangler deploy
   ```
   （パスは環境に合わせて変更。未ログインなら先に `npx wrangler login`）

3. キュー `podcast-generate` は、Consumer の設定があると **自動作成** される場合があります。  
   自動作成されない場合は、次で作成してから再度デプロイする。
   ```bash
   npx wrangler queues create podcast-generate
   ```

4. ブラウザで「選択したキャスターで生成する」を実行し、数分以内に「生成完了」になるか確認する。

## 変更したファイル一覧

| ファイル | 変更内容 |
|---------|----------|
| `worker/wrangler.toml` | `[[queues.producers]]` と `[[queues.consumers]]` を追加 |
| `worker/src/index.ts` | `Env` に `PODCAST_QUEUE` を追加／`handleGenerate` と `scheduled` でキュー送信に変更／`queue()` ハンドラを追加 |

これで、西村哲以外のキャスターや「全員分まとめて生成」も、途中で止まらず完了する想定です。
